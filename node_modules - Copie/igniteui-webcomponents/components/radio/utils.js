import { isDefined, iterNodes } from '../common/util.js';
export function getGroup(member) {
    const result = {
        active: [],
        checked: [],
        radios: [],
        siblings: [],
    };
    if (!isDefined(globalThis.document)) {
        return result;
    }
    const iterator = iterNodes(globalThis.document.documentElement, 'SHOW_ELEMENT', (radio) => radio.matches(`${member.tagName}[name='${member.name}']`));
    for (const each of iterator) {
        result.radios.push(each);
        if (!each.disabled) {
            result.active.push(each);
        }
        if (each.checked) {
            result.checked.push(each);
        }
        if (each !== member) {
            result.siblings.push(each);
        }
    }
    return result;
}
//# sourceMappingURL=utils.js.map